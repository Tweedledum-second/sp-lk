spring:
  application:
    name: sp-lk
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/spdb
    driverClassName: org.postgresql.Driver
    username: postgres
    password: Trulala+Tralala1